<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Editar Registro</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body class="auth-body">
  <div class="form-container">
    <h2>Editar Registro</h2>
    <form method="POST" action="/edit/<%= registro.id %>">
      <input type="date" name="date" value="<%= registro.date %>" required>
      <input type="text" name="description" value="<%= registro.description %>" required>
      <input type="text" name="category" value="<%= registro.category %>" required>
      <select name="type">
        <option value="entrada" <%= registro.type === 'entrada' ? 'selected' : '' %>>Entrada</option>
        <option value="saida" <%= registro.type === 'saida' ? 'selected' : '' %>>Saída</option>
      </select>
      <label><input type="checkbox" name="fixed" <%= registro.fixed ? 'checked' : '' %>> Fixa</label>
      <label><input type="checkbox" name="paid" <%= registro.paid ? 'checked' : '' %>> Paga</label>
      <input type="number" step="0.01" name="amount" value="<%= registro.amount %>" required>
      <button type="submit">Salvar Alterações</button>
    </form>
    <a href="/" class="btn-back">Voltar</a>
  </div>
</body>
</html>
